PORT=5000

# Local MongoDB, e.g. mongodb://127.0.0.1:27017/rosea
# or an Atlas connection string.
# Leave unset and the server still runs — /api/health will just report db.state = "disconnected".
MONGODB_URI=
